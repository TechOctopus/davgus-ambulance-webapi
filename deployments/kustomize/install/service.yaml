kind: Service
apiVersion: v1
metadata:
  name: davgus-ambulance-webapi
spec:
  selector:
    pod: davgus-ambulance-webapi-label
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: webapi-port
